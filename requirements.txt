# BMS Estimation Tool — Python dependencies
# pip install -r requirements.txt

# Core AI
anthropic>=0.25.0
openai>=1.0.0

# Streamlit UI
streamlit>=1.35.0

# PDF processing
pymupdf>=1.24.0          # fitz — fast native text extraction
pdfplumber>=0.11.0       # table-aware extraction
pdf2image>=1.17.0        # PDF→image for OCR fallback
pytesseract>=0.3.10      # OCR fallback (requires Tesseract binary)

# Document processing
python-docx>=1.1.0       # .docx input for specs/SOO

# Data
pandas>=2.2.0
xlsxwriter>=3.2.0        # Excel output
openpyxl>=3.1.0

# Utilities
Pillow>=10.0.0
